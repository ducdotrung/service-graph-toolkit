# Claude Adapter

Follow `shared/AGENTS.md`; load matching `skills/` on demand. Run
`scripts/context-pack.sh` before broad exploration. Use `scripts/fetch-web.mjs`
for known public URLs and local GitNexus stdio configuration from `graph.py`.
