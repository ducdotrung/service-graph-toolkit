# Pi Adapter

Use `shared/AGENTS.md` and `shared/MODEL_GUIDE.md` as the provider-neutral
policy. Pi may translate the model guide into subagent overrides. Prefer calling
the portable scripts here over requiring a Pi-only extension.
