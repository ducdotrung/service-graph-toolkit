# Memory

This folder keeps reusable working structure for future sessions.

Use it for:

- `sessions/`: short logs of what changed in one work session
- `decisions/`: durable design or workflow choices
- `gotchas/`: traps worth preserving
- `prompts/`: reusable handoff or operating prompts

Keep platform facts in `inventory.yaml`. Use memory only for the work around
that inventory.
